import 'package:flutter/material.dart';

class ScrollListWrapper extends StatelessWidget {
  const ScrollListWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
